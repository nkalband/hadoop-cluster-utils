
${HADOOP_HOME?"HADOOP_HOME not initialized. Check .bashrc file"} 2>/dev/null

echo -n "Copy $1 to "
cat ${HADOOP_HOME}/etc/hadoop/slaves | grep -v ^# | xargs -i echo -n "{}, "
echo ""

cat ${HADOOP_HOME}/etc/hadoop/slaves | grep -v ^# | xargs -i scp $1 {}:$2

